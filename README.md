# fxyoge/features

This is a collection of my own personal devcontainer features. They probably aren't useful for your direct consumption, so consider vendoring instead:

```sh
git subtree add --prefix .devcontainer/features/fxyoge-features git@github.com:fxyoge/features.git main --squash
```
